# {{ID}}

Adaptation snippet for `{{ID}}`. Use after shortlisting from `references/index.jsonl` and `references/example-digests.md`. This is not the canonical full example; trust the linked runnable example for verification.

```css
/* Replace with the smallest useful CSS/HTML kernel for {{TITLE}}. */
```
