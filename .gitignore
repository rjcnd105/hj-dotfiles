result

.direnv

templates/**/.devenv*
templates/**/devenv.local.nix
templates/**/.direnv
templates/**/.pre-commit-config.yaml


**/nix.secrets.conf

**/keys.txt

files/**/profiles/session/*

# Agent Package Manager install cache; manifests, sources, locks, and generated
# target files remain tracked.
/files/workspace/apm_modules/

# Claude Code synced-skills cache, written through the ~/.claude/skills link.
# Regenerated by Claude sync; never source.
/files/workspace/.claude/skills/synced/
