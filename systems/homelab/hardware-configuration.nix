# 이 파일은 placeholder입니다.
# NixOS 설치 시 아래 명령으로 생성된 내용으로 교체하세요:
#   nixos-generate-config --root /mnt
#
# 생성 파일에는 fileSystems, swapDevices, boot.initrd,
# hardware.*, nixpkgs.hostPlatform 등이 포함됩니다.
{ ... }:
{
  # placeholder — nixos-generate-config 결과로 교체 필요
  nixpkgs.hostPlatform = "x86_64-linux";
}
