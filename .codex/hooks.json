{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v graphify >/dev/null 2>&1; then graphify hook-check; elif [ -x \"$HOME/.local/share/mise/installs/pipx-graphifyy/latest/bin/graphify\" ]; then \"$HOME/.local/share/mise/installs/pipx-graphifyy/latest/bin/graphify\" hook-check; fi"
          }
        ]
      }
    ]
  }
}
